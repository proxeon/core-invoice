# Accessibility contract (CLI)

Validity is never colour-only. The CLI prints `valid (profile)` or finding ids as text (and JSON). Exit codes 0/1/2 are the machine contract. Do not add a colour-only verdict.
