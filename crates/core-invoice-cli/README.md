# core-invoice-cli

```sh
cargo run -p core-invoice-cli -- validate --profile pint-my invoice.xml
cargo run -p core-invoice-cli -- convert invoice.xml --to cii
cargo run -p core-invoice-cli -- diff a.xml b.xml
cargo run -p core-invoice-cli -- explain PINT-TAX
```
