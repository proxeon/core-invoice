# Corpus (synthetic, MIT OR Apache-2.0)

Do **not** copy CEN EUPL / Peppol example XML here. Official samples used by tests live in git-tracked `testdata/` (see `testdata/NOTICE`). Full clones stay under gitignored `refers/` (`task spec`). Do **not** invent a PINT Billing 1.1.2 instance XML here — that pin ships none.

Mutation-per-rule oracles are in crate tests (`category`, `peppol`, `codes`, `reconcile`). This folder is for authored documents we can ship.
